<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.mycompany.Tget_mini_web.dao.mybatis.Cart">




	<!-- 장바구니 리스트에 수에 따라 페이징 처리하기 -->
 	<select id="selectByPage" parameterType="PagerDto" resultType="CartDto">
		<!-- C는 캐릭터를 의미. 문자 그대로의 데이터를 받아들이는 구문 -->
		<![CDATA[
			select rnum, cno, mid
			from (
			    select rownum as rnum, cno, mid
			    from (
			        select *
			        from cart
			        order by cno desc
			    )
			    where rownum <= #{endRowNo}
			)
			where rnum >= #{startRowNo}
		]]>
	</select>
	
	
</mapper>